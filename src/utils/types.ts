export interface ICar {
    id: string
    name: string
    model: string
    year: number
    color: string
    price: number
    latitude: number
    longitude: number
}

export type TCoordinates = [number, number][]
